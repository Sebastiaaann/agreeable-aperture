---
---
<section class="hero hero--with-bg" id="inicio" data-animate="fade-up" data-duration="900" data-initial>
  <picture class="hero-media">
    <!-- Fuentes alternativas (añadir /2.avif y /2.webp en /public cuando estén disponibles). Navegadores sin soporte caen al JPG -->
    <img
      src="/2.jpg"
      alt="Camión de Fletes Marcelo listo para transporte"
      loading="eager"
      fetchpriority="high"
      decoding="async"
      width="1920"
      height="1080"
      srcset="/2.jpg 800w"
      sizes="100vw"
      style="object-position:center;"
    />
  </picture>
  <div class="hero-overlay"></div>
  <div class="container hero-content">
  <h1 data-animate="fade-up" data-delay="80" data-initial>Fletes y Transportes en Puerto Montt & Región de Los Lagos</h1>
  <p data-animate="fade-up" data-delay="160" data-initial>Servicio profesional de fletes, mudanzas, transporte de materiales y encomiendas. Puntualidad, seguridad y trato cercano con más de 4 años de experiencia.</p>
  <div class="hero-cta" data-animate="zoom-in" data-delay="260" data-initial>
  <a href="#contacto" class="btn-pill-quote" aria-label="Solicitar cotización por formulario (sección contacto)">
        <span class="pill-icon" aria-hidden="true">📦</span>
        <span class="pill-text">Solicitar cotización</span>
        <span class="pill-arrow" aria-hidden="true">➜</span>
      </a>
      <a href="#servicios" class="btn btn-dark">Ver Servicios</a>
    </div>
  </div>
</section>
<style>
.hero--with-bg { position: relative; padding: clamp(5rem, 12vh, 8rem) 0 4rem; overflow: hidden; }
.hero-media { position:absolute; inset:0; width:100%; height:100%; }
.hero-media img { width:100%; height:100%; object-fit:cover; object-position:center; }
.hero-overlay { position:absolute; inset:0; background:linear-gradient(180deg, rgba(15,18,26,0.55), rgba(15,18,26,0.80)); mix-blend-mode: multiply; }
.hero-content { position:relative; z-index:2; text-align:center; color:#fff; }
.hero-content h1 { text-shadow:0 4px 18px rgba(0,0,0,.45); max-width:58ch; margin-inline:auto; }
.hero-content p { max-width:60ch; margin-inline:auto; }
/* Botón pill personalizado */
.btn-pill-quote { --pill-bg:#fff; --pill-color:#0f121a; --pill-border:rgba(0,0,0,.08); display:inline-flex; align-items:center; gap:.75rem; background:var(--pill-bg); color:var(--pill-color); padding:.65rem 1.15rem .65rem .85rem; border-radius:1000px; font-weight:600; font-size:.92rem; letter-spacing:.4px; position:relative; line-height:1; box-shadow:0 4px 14px rgba(0,0,0,.25); border:1px solid var(--pill-border); text-decoration:none; transition: box-shadow .35s, transform .35s, background .35s; }
.btn-pill-quote .pill-icon { background:linear-gradient(135deg,#f6d9a3,#c2b280); color:#222; width:34px; height:34px; border-radius:50%; display:flex; align-items:center; justify-content:center; font-size:1.05rem; box-shadow:0 2px 6px rgba(0,0,0,.25) inset, 0 1px 2px rgba(255,255,255,.6); }
.btn-pill-quote .pill-text { white-space:nowrap; }
.btn-pill-quote .pill-arrow { background:#0f121a; color:#fff; width:32px; height:32px; display:flex; align-items:center; justify-content:center; border-radius:50%; font-size:.95rem; transition:background .35s, transform .35s; box-shadow:0 2px 6px rgba(0,0,0,.4) inset; }
.btn-pill-quote:hover { box-shadow:0 6px 22px rgba(0,0,0,.45); transform:translateY(-3px); }
.btn-pill-quote:hover .pill-arrow { background:#222a36; transform:translateX(2px); }
.btn-pill-quote:active { transform:translateY(-1px) scale(.98); }
.btn-pill-quote:focus-visible { outline:3px solid #c2b280; outline-offset:3px; box-shadow:0 0 0 4px rgba(194,178,128,.35); }
@media (max-width:560px){
  .btn-pill-quote { font-size:.85rem; padding:.55rem .95rem .55rem .7rem; }
  .btn-pill-quote .pill-icon { width:30px; height:30px; font-size:.95rem; }
  .btn-pill-quote .pill-arrow { width:28px; height:28px; }
}
/* Layout CTA y responsividad adicional */
.hero-cta { display:flex; align-items:center; justify-content:center; gap:1rem; flex-wrap:wrap; }
@media (max-width:520px){
  .hero-cta { flex-direction:column; align-items:stretch; }
  .hero-cta a { width:100%; justify-content:center; }
}
</style>
