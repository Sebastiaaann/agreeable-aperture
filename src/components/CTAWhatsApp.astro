---
// Componente: CTAWhatsApp (Bloque de llamada a la acción principal)
const phone = '+56912345678'; // Ajustar al número real
const message = encodeURIComponent('Hola, quisiera una cotización de flete.');
const waLink = `https://wa.me/${phone.replace(/[^0-9]/g,'')}?text=${message}`;
---
<section class="cta-wsp" aria-labelledby="cta-wsp-title" data-animate="fade-up">
  <div class="cta-wsp__inner container">
    <div class="cta-wsp__text">
      <h2 id="cta-wsp-title" class="cta-wsp__title">¿Necesitas tu cotización ahora mismo?</h2>
      <p class="cta-wsp__desc">Escríbenos por WhatsApp y te respondemos en minutos con una propuesta clara y sin sorpresas.</p>
      <a href={waLink} target="_blank" rel="noopener" class="cta-wsp__btn" data-animate="zoom-in" data-delay="140">Hablar por WhatsApp</a>
    </div>
  </div>
</section>
<style>
  .cta-wsp { position:relative; background:linear-gradient(140deg,#1c2130,#252d40); color:#fff; padding:3.6rem 0 3.4rem; overflow:hidden; }
  .cta-wsp::before { content:""; position:absolute; inset:0; background:radial-gradient(circle at 75% 15%, rgba(255,255,255,.12), transparent 60%); opacity:.5; pointer-events:none; }
  .cta-wsp__text { max-width:780px; margin:0 auto; text-align:center; position:relative; }
  .cta-wsp__title { margin:0 0 1rem; font-size:clamp(1.6rem,3vw,2.1rem); font-weight:700; letter-spacing:.5px; }
  .cta-wsp__desc { margin:0 0 1.8rem; font-size:.98rem; line-height:1.5; opacity:.9; }
  .cta-wsp__btn { --btn-bg:#c2b280; --btn-bg-h:#d2c28f; display:inline-flex; align-items:center; gap:.55rem; padding:.95rem 1.55rem; background:var(--btn-bg); color:#1c2130; font-weight:700; border-radius:50px; font-size:.9rem; letter-spacing:.5px; text-decoration:none; box-shadow:0 4px 14px rgba(0,0,0,.3); transition:background .35s, transform .35s; }
  .cta-wsp__btn:hover { background:var(--btn-bg-h); transform:translateY(-3px); }
  .cta-wsp__btn:active { transform:translateY(-1px); }
  @media (max-width:680px){ .cta-wsp { padding:3.1rem 0 3rem; } .cta-wsp__desc { font-size:.9rem; } }
</style>
